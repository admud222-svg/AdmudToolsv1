{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.melonbp_brp.generic.move": {
      "initial_state": "idle",
      "states": {
        "idle": {
          "animations": ["idle"],
          "transitions": [{ "walking": "q.ground_speed > 0.1 && variable.attack_time == 0" }],
          "blend_transition": 0.2
        },
        "walking": {
          "animations": ["walk"],
          "transitions": [{ "idle": "q.ground_speed < 0.1 || variable.attack_time > 0" }],
          "blend_transition": 0.2
        }
      }
    },
    "controller.animation.melonbp_brp.generic.attack": {
      "states": {
        "default": {
          "transitions": [{ "active": "variable.attack_time > 0" }]
        },
        "active": {
          "animations": ["attack"],
          "blend_transition": 0.2,
          "transitions": [{ "default": "query.all_animations_finished" }]
        }
      }
    }
  }
}
